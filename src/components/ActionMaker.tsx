import React from "react";

import { Box } from "@mui/material";

type Props = {
  group: string;
  status: string;
};

const ActionMaker = (props: Props) => {
  return <Box></Box>;
};

export default ActionMaker;
